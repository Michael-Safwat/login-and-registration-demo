registration api url http://localhost:8080/api/v1/registration
to use the email service: first install maildev using the following command (note: you will need node installed):
      npm install -g maildev
      maildev
  
